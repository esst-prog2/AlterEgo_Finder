# Planning Log

One line per decision: date, what was decided, who decided it. Append-only — never edit or remove earlier lines.

- 2026-09-22: Adopted this log — every project decision (requirement, number, name, tool) gets one append-only line here. Decided by user.
- 2026-09-22: Adopted OpenSpec CLI (@fission-ai/openspec) v1.13.0 as the spec-driven workflow tool for this repo, configured for Claude Code; Node.js 24.19.0 LTS installed as a prerequisite. Decided by user.
- 2026-09-22: Added AGENTS.md at repo root summarizing project status, the OpenSpec workflow, and the PLANNING_LOG.md convention. Decided by user.
- 2026-09-22: Restructured the planning-log rule in AGENTS.md as a "## Planning log" section (not a blockquote) and added CLAUDE.md as a one-line pointer (`@AGENTS.md`) so Claude Code loads it. Decided by user.
- 2026-09-22: Set global git identity to user.name "prokof2123", user.email "prokof21@gmail.com" (previously unset at both local and global scope). Decided by user.
- 2026-09-22: Chose OpenCV DNN + OpenFace (nn4.small2.v1) for the 128-d face embedding, over face_recognition/dlib, to keep install easy for a grader on Windows (pip-installable prebuilt wheel, no CMake/build tools). Decided by user.
- 2026-09-22: Chose OpenCV's DNN-based face detector (Caffe SSD, res10_300x300) to pair with OpenFace, keeping the same install story (no extra dependency). Decided by user.
- 2026-09-22: data/celebrities/ uses the standard LFW layout, one subfolder per person (data/celebrities/<person_name>/<image_name>.jpg), so match attribution is Path(match).parent.name with no separate metadata/mapping file. Decided by user.
- 2026-09-22: The 0.68 threshold is a configurable CLI parameter of match.py (--threshold, default 0.68), pre-calculated offline by a separate calibrate.py script rather than recomputed at runtime, to keep match.py under the 100ms budget. Decided by user.
- 2026-09-22: calibrate.py writes its computed threshold to data/config.json ({"threshold": 0.68}); match.py loads its --threshold default from that file if present, with manual CLI override still allowed, so the two scripts stay in sync without hand-copying. Decided by user.
- 2026-09-22: data/calibration/ holds a set of LFW identities fully disjoint from data/celebrities/; calibrate.py computes intra-/inter-class distances only on data/calibration/, so match.py's search index never sees the calibration identities. Decided by user.
- 2026-09-22: Confirmed the three-change split (core pipeline / calibration / report) and captured Change 1 as the OpenSpec change "add-face-matching-cli" (proposal, design, two capability specs face-embedding + face-matching-cli, tasks) — validated, ready for implementation. Decided by user.
- 2026-09-22: Within add-face-matching-cli's design, chose to vendor the ~40MB of pretrained model weights (OpenCV SSD detector + OpenFace nn4.small2.v1) directly in the repo rather than download them at first run, to preserve the project's offline-only claim and avoid a network failure mode. Decided by agent.
- 2026-09-22: Implemented add-face-matching-cli end to end (all 16 tasks): match.py CLI, face_pipeline/{detector,embedder,index}.py, vendored model weights, requirements.txt/-dev.txt, and 15 passing tests. Loosened numpy's pin from <2 to unpinned after discovering numpy<2 has no Python 3.13 wheel on Windows and would otherwise try to compile from source. Decided by agent.
- 2026-09-22: Synced add-face-matching-cli's delta specs into openspec/specs/{face-embedding,face-matching-cli}/spec.md as the project's first durable capability specs, and archived the change to openspec/changes/archive/2026-09-22-add-face-matching-cli/. Decided by user.
- 2026-09-22: Resolved the HIGH_CONFIDENCE/LOW_CONFIDENCE/NO_MATCH ambiguity in README by using two calibrated thresholds (not one): a low threshold at the EER point (the existing 0.68 concept) and a high threshold at a low-false-accept-rate operating point on the same ROC curve. Decided by user.
- 2026-09-22: Set the high-confidence threshold's false-accept-rate target at 1% (a standard, conservative face-verification operating point) as a named constant in face_pipeline/calibration.py, not derived from README. Decided by agent.
- 2026-09-22: data/config.json's schema is {"threshold_low": ..., "threshold_high": ...}, superseding the earlier single-key {"threshold": 0.68} sketch now that two thresholds are needed. match.py errors out (rather than guessing) if neither data/config.json nor both --threshold-low/--threshold-high CLI flags are available. Decided by agent.
- 2026-09-22: Scaffolded and planned Change 2 as the OpenSpec change "add-threshold-calibration" (proposal, design, threshold-calibration spec, face-matching-cli delta spec, tasks) — validated, ready for implementation. Decided by user.
- 2026-09-22: Implemented add-threshold-calibration end to end (all 13 tasks): calibrate.py CLI, face_pipeline/calibration.py (pair sampling, EER, FAR-based threshold search), match.py's --threshold-low/--threshold-high/--config options and per-candidate HIGH_CONFIDENCE/LOW_CONFIDENCE/NO_MATCH labels. Added a calibration test fixture (Kit Harington, Rose Leslie — disjoint from the Obama/Biden matching fixtures) from the same face_recognition repo used for change 1's fixtures. 30/30 tests passing. Decided by agent.
- 2026-09-22: Corrected the git workflow order for archiving: implementation is now committed and pushed separately from the archive+spec-sync commit, rather than bundling both into one push. Redid add-face-matching-cli's archive under this pattern (unarchive, push, re-archive, push), then archived add-threshold-calibration the same way, syncing its MODIFIED face-matching-cli requirement and new threshold-calibration capability into the main specs. Decided by user.
- 2026-09-22: Added a run-alterego-finder Claude Code skill (.claude/skills/run-alterego-finder/) with a Python driver that runs calibrate.py + match.py end to end against tests/fixtures/, verified line-by-line from both PowerShell and Git Bash. Documents a real gotcha: cv2.imwrite silently fails on a hand-typed POSIX path inside inline Python run via Git Bash. Decided by user.
- 2026-09-22: Refined the planning-log attribution: going forward, entries distinguish "Decided by user" (user originated it), "Proposed by agent, accepted by user" (agent proposed, user accepted), and "Decided by agent" (agent decided unilaterally) instead of collapsing the middle case into "Decided by user". Applies only to new entries; earlier lines are not rewritten. Decided by user.
- 2026-09-22: Reopened the embedding-model choice: the 128-d requirement in add-face-matching-cli's design.md was a design artifact, not an actual project requirement — README only used 128-d illustratively, and nothing in section 4's acceptance criteria depends on that number. Switching to an ArcFace-family model (512-d, ~99%+ LFW verification accuracy vs OpenFace nn4.small2.v1's ~93%), loaded as a standalone ONNX file via cv2.dnn.readNetFromONNX (not the insightface package, which has real Windows/Python 3.13 pip-install friction) to keep install effort unchanged. Requires adding a face-alignment step (5-point landmark warp), which the current pipeline lacks for either model. Decided by user.
- 2026-09-22: Picked ArcFace ResNet100 INT8 (~63MB) over the FP32 variant (~249MB) — same 512-d ArcFace-family accuracy characteristics, far more reasonable to vendor/fetch. Split the ArcFace work into two changes: switch-to-arcface-embedding (model swap, alignment, recalibration) and fetch-model-weights-checksum (asset delivery + README cleanup), to keep each change's diff reviewable. Decided by user.
- 2026-09-22: Scaffolded and planned switch-to-arcface-embedding as an OpenSpec change (proposal, design, MODIFIED face-embedding spec, tasks) — validated, ready for implementation. Detector: YuNet via cv2.FaceDetectorYN (bbox + 5 landmarks in one pass, OpenCV's own officially bundled model). Alignment: standard ArcFace 5-point reference template via cv2.estimateAffinePartial2D/warpAffine, with explicit care around YuNet's landmark order (right eye, left eye, nose, right mouth, left mouth) differing from the reference template's order (left eye, right eye, nose, left mouth, right mouth). Decided by user.
- 2026-09-22: Implemented switch-to-arcface-embedding end to end (all 14 tasks): YuNet detector (face_pipeline/detector.py), new face_pipeline/alignment.py, ArcFace ResNet100 INT8 embedder (512-d), vendored models/{face_detection_yunet_2023mar.onnx,arcfaceresnet100-11-int8.onnx}, old 128-d model files removed. 33/33 tests passing, plus a full driver run (calibrate.py -> match.py) confirming dramatically cleaner same/different-person separation than the old pipeline (~0.78-0.80 vs ~-0.07, versus the old pipeline's ~0.64-0.71 range). Found and corrected two genuine bugs via empirical verification (not trusting docs): (1) the landmark-pairing scheme this change's own design.md specified was backwards — YuNet's raw landmark order actually maps directly, index-for-index, onto the ArcFace reference template, verified by fitting a transform and checking per-point residual (2.6px correct vs ~20px and a visibly rotated face under the originally-planned re-pairing); (2) the commonly-documented (pixel-127.5)/128 preprocessing collapses this specific ONNX Model Zoo model's same/different-person similarity into the same ~0.95 band with no separation — confirmed on both INT8 and FP32 (ruling out quantization as the cause) — raw, unnormalized RGB pixels is what this model actually expects. Also found and removed a stray gitignored .matchcache/ inside tests/fixtures/dataset/ from earlier manual testing that was silently corrupting test runs via shutil.copytree. Decided by agent.
- 2026-09-22: Refreshed README.md (issue #2 from the original 3-issue list): removed the hardcoded 0.68 threshold (now described as dynamically calibrated via calibrate.py, no fixed number in the doc), removed the hardcoded 128-d embedding dimension in favor of generic "fixed-length embedding" language (to avoid the doc going stale again next time the model changes), described the two-threshold HIGH/LOW/NO_MATCH scheme accurately, and reconciled the NO_RELIABLE_MATCH/NO_MATCH naming inconsistency to NO_MATCH (matching the actual implemented label). Decided by user.
- 2026-09-22: Synced switch-to-arcface-embedding's MODIFIED face-embedding requirement (128-d -> 512-d) into the main spec, and archived the change to openspec/changes/archive/2026-09-22-switch-to-arcface-embedding/. Decided by user.
- 2026-09-22: Scaffolded and planned fetch-model-weights-checksum as an OpenSpec change (proposal, design, new model-weight-delivery spec, tasks) — validated, ready for implementation. Manifest-driven design: models/manifest.json is the single source of truth for filename/url/sha256, read by a new face_pipeline/weights.py; stdlib urllib.request only (no new dependency); checksum re-verified on every use, not just after download. Decided by user.
- 2026-09-22: Implemented fetch-model-weights-checksum end to end (all 10 tasks): face_pipeline/weights.py (download-if-missing + SHA-256 verify + clear ModelWeightError), models/manifest.json as the single source of truth, detector.py/embedder.py wired to call ensure_weight() before loading, models/*.onnx removed from git tracking (git rm --cached, added to .gitignore, files kept on disk), models/README.md rewritten to describe fetch-on-first-run instead of vendoring. Manually verified the real download path end to end (both files moved aside, re-downloaded via the actual network in ~22s, byte-identical checksums, full driver pipeline produced identical results). 37/37 tests passing. Decided by agent.
- 2026-09-22: Deferred a live drag-and-drop Gradio UI in favor of building summary.html first — it's explicitly in README's "First useful version" scope (the one remaining unimplemented MVP bullet) and needs no new heavy dependency, unlike Gradio (FastAPI/Uvicorn). Gradio, not Streamlit or hand-rolled Flask, is the right choice if a live UI is wanted later. Proposed by agent, accepted by user.
- 2026-09-22: Scaffolded and planned add-summary-report as an OpenSpec change (proposal, design, new summary-report spec, ADDED face-matching-cli requirement, tasks) — validated, ready for implementation. Plain f-string HTML templating (no Jinja2), images embedded as base64 JPEG data URIs via cv2.imencode, results capped to a REPORT_TOP_K=10 constant, --report-out defaults to the literal data/results/summary.html (not derived from --dataset). Decided by user.
- 2026-09-22: Implemented add-summary-report end to end (all 7 tasks): face_pipeline/report.py (self-contained HTML rendering, base64-embedded images, color-coded confidence labels, top-10 cap), match.py's --report-out wiring (written only on successful match, parent directory auto-created). Caught and fixed a real hermeticity bug before it reached a commit: the existing match_cli test suite didn't pass --report-out, so every test run was writing a real data/results/summary.html into the actual repo directory; fixed by pointing all success-path tests at tmp_path. 43/43 tests passing; also ran the real CLI end-to-end to confirm a well-formed report renders correctly (structurally verified; no browser-automation tool available in this environment to screenshot it). This completes README section 3's "First useful version" MVP scope. Decided by agent.
- 2026-09-22: Synced fetch-model-weights-checksum's new model-weight-delivery capability into the main specs, and archived the change to openspec/changes/archive/2026-09-22-fetch-model-weights-checksum/. Decided by user.
